# ==============================================================================
# ShipFlowX Enterprise CI/CD Pipeline - Infrastructure Deployment
# ==============================================================================
# This pipeline orchestrates Terraform infrastructure deployments in 4 distinct 
# stages (Init, Validate, Plan, Apply) using local state execution.
# ==============================================================================

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - terraform/*

pool:
  vmImage: 'ubuntu-latest'

# ==============================================================================
# Pipeline Variables
# ==============================================================================
variables:
  # The name of the Azure Resource Manager Service Connection configured in Azure DevOps
  azureServiceConnection: 'ShipFlowX-Azure'
  terraformVersion: 'latest'

stages:
  # ==========================================================================
  # Stage 1: Terraform Init
  # ==========================================================================
  - stage: Terraform_Init
    displayName: 'Terraform Init'
    jobs:
      - job: Init
        displayName: 'Terraform Init'
        steps:
          # Install official Terraform binary on the host agent
          - task: TerraformInstaller@1
            displayName: 'Install Terraform'
            inputs:
              terraformVersion: '$(terraformVersion)'

          # Initialize using standard script to bypass AzureRM backend validations
          - script: |
              terraform version
              cd terraform
              terraform init -backend=false
              echo "Terraform initialized successfully."
            displayName: 'Run terraform init'

  # ==========================================================================
  # Stage 2: Terraform Validate
  # ==========================================================================
  - stage: Terraform_Validate
    displayName: 'Terraform Validate'
    dependsOn: Terraform_Init
    jobs:
      - job: Validate
        displayName: 'Terraform Validate'
        steps:
          # Install official Terraform binary on the host agent
          - task: TerraformInstaller@1
            displayName: 'Install Terraform'
            inputs:
              terraformVersion: '$(terraformVersion)'

          # Validate syntax on local backend
          - script: |
              terraform version
              cd terraform
              terraform init -backend=false
              terraform validate
              echo "Terraform syntax validation succeeded."
            displayName: 'Run terraform validate'

  # ==========================================================================
  # Stage 3: Terraform Plan
  # ==========================================================================
  - stage: Terraform_Plan
    displayName: 'Terraform Plan'
    dependsOn: Terraform_Validate
    jobs:
      - job: Plan
        displayName: 'Terraform Plan'
        steps:
          # Install official Terraform binary on the host agent
          - task: TerraformInstaller@1
            displayName: 'Install Terraform'
            inputs:
              terraformVersion: '$(terraformVersion)'

          # Run plan using the ARM service connection context for Azure provider auth
          - task: AzureCLI@2
            displayName: 'Run terraform plan'
            inputs:
              azureSubscription: '$(azureServiceConnection)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                # Create a global symlink so the Azure CLI shell context can locate the installed binary
                sudo ln -sf $(which terraform) /usr/bin/terraform || true
                terraform version
                
                cd terraform
                terraform init -backend=false
                terraform plan -out=$(Build.ArtifactStagingDirectory)/tfplan

          - task: PublishPipelineArtifact@1
            displayName: 'Publish Plan Artifact'
            inputs:
              targetPath: '$(Build.ArtifactStagingDirectory)/tfplan'
              artifactName: 'tfplan-artifact'
              publishLocation: 'pipeline'

  # ==========================================================================
  # Stage 4: Terraform Apply
  # ==========================================================================
  - stage: Terraform_Apply
    displayName: 'Terraform Apply'
    dependsOn: Terraform_Plan
    condition: succeeded()
    jobs:
      - job: Apply
        displayName: 'Terraform Apply'
        steps:
          - task: DownloadPipelineArtifact@2
            displayName: 'Download Plan Artifact'
            inputs:
              buildType: 'current'
              artifactName: 'tfplan-artifact'
              targetPath: '$(Pipeline.Workspace)/tfplan-artifact'

          # Install official Terraform binary on the host agent
          - task: TerraformInstaller@1
            displayName: 'Install Terraform'
            inputs:
              terraformVersion: '$(terraformVersion)'

          # Apply using the ARM service connection context
          - task: AzureCLI@2
            displayName: 'Run terraform apply'
            inputs:
              azureSubscription: '$(azureServiceConnection)'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                # Create a global symlink so the Azure CLI shell context can locate the installed binary
                sudo ln -sf $(which terraform) /usr/bin/terraform || true
                terraform version
                
                cd terraform
                terraform init -backend=false
                terraform apply -auto-approve $(Pipeline.Workspace)/tfplan-artifact/tfplan
